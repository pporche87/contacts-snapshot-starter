<nav class="navbar navbar-static-top">
	<div class="navbar-brand">
		<a href="/">Home</a>
		<form method="get" action="/contacts/search" class="search-form">
			<div>
				<input name="q" type="search" placeholder="search" value="<%= query %>" autofocus/>
				<input type="submit" style="display: none" />
			</div>
		</form>
		<a href="/contacts/new">New Contact</a>
	</div>
	<div class="navbar-right">
		<% if (locals.isLoggedIn) { %>
			<a class="btn btn-default auth" href="/logout">Log Out</a>
		<% } else { %>
			<a class="btn btn-default auth" href="/signup">Sign Up</a>
			<a class="btn btn-default auth" href="/login">Login</a>
		<% } %>
	</div>

</nav>
